import {StyleSheet} from "react-native";

const Calendar_Style = StyleSheet.create({

    container: {
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
    },

})

export {Calendar_Style}